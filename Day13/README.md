# Log Analyzer Script

A shell script that analyzes log files and generates a report.

## Features
- Counts ERROR, WARNING, and INFO messages
- Extracts unique IP addresses
- Generates timestamped report

## Usage

```bash
chmod +x log_analyzer.sh
./log_analyzer.sh app.log
